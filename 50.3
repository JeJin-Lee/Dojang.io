<50.3>
distance = sqrt(pow(p1.x-p2.x,2)+pow(p1.y-p2.y,2));

<51.6>
int a, b;

<51.7>
#pragma pack(push,1)
struct Packet{
    char a;
    short b;
};

<52.6>
&p1,0,sizeof(struct Person)

<52.7>
&p2, p1, sizeof(struct Person)
